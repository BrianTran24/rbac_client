name: rbac_client
description: Reusable RBAC annotations, runtime contracts, and code generator.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  build: ^2.4.1
  source_gen: ^1.3.2
  analyzer: ^6.4.1

dev_dependencies:
  flutter_lints: ^6.0.0
  dart_style: ^2.3.0
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
