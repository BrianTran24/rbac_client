## 0.1.0

- extract reusable RBAC runtime contracts into the package
- move `Access` annotation into the package
- make generated wrappers depend on generic session/context adapters instead of app-specific Zhin types
